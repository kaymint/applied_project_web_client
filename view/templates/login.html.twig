{% extends "index.html.twig" %}

{% block NAVIGATION_MENU %}
    <div class="login_logo text-center">
        <img src="img/320.png">
    </div>

{% endblock %}

{# Override sidebar#}
{% block SIDEBAR %}

{% endblock %}


{% block MAINCONTENT %}
    <div class="container">
    <form class="form-horizontal login_form" action="../controller/driver-controller.php" method="post">
        <fieldset>
            <legend class="text-center">LOGIN</legend>
            {% if message is defined %}
                <h1 class="alert-danger">Invalid User</h1>
            {% endif %}
            <input type="hidden" value="1" name="cmd">
            <div class="form-group">
                <label for="inputPIN" class="col-xs-2 control-label">DRIVER PIN</label>
                <div class="col-lg-10">
                    <input type="text" class="form-control" id="inputPIN" placeholder="PIN" name="PIN">
                </div>
            </div>
            <div class="form-group">
                <div class="col-lg-10 col-lg-offset-2">
                    <button type="reset" class="btn btn-default">Cancel</button>
                    <button type="submit" class="btn btn-primary">LOGIN</button>
                </div>
            </div>
        </fieldset>
    </form>
    </div>

{% endblock %}